package repository

import (
	"go.uber.org/fx"
)

/* ========================================================================
 * Repository Module
 * ========================================================================
 * 职责: 提供仓储依赖注入模块
 * ======================================================================== */

// Module 仓储模块
// 注意: 由于 RepositoryImpl 是泛型的，无法直接提供具体实例，
// 业务服务应自行定义具体的 Repo 并注入，或者使用此模块提供的基础支持。
var Module = fx.Module("repository",
	// 导出基础构造函数
	fx.Provide(func() interface{} {
		// 占位
		return nil
	}),
)
