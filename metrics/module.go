package metrics

import (
	"go.uber.org/fx"
)

/* ========================================================================
 * Metrics Module
 * ========================================================================
 * 职责: 提供 Prometheus 指标依赖注入模块
 * ======================================================================== */

// Module 指标模块
var Module = fx.Module("metrics",
	// Metrics 通常在 HTTP Server 启动时自动注册，此处导出一些自定义 Collector (如有)
	fx.Invoke(func() {
		// 执行一些初始化的指标注册
	}),
)
