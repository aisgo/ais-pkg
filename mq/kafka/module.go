package kafka

/* ========================================================================
 * Kafka Module
 * ========================================================================
 * 职责: 提供 Kafka Consumer/Producer 依赖注入模块
 * 注意: 由于 MQ Factory 模式的存在，通常通过 mq 包统一注册，
 * 这里主要提供直接使用 sarama 客户端的场景（如果有）或特定配置。
 * 目前 mq 包通过 init() 自动注册工厂，因此这里暂时不需要显式提供 Module
 * 除非我们需要将底层 sarama 对象暴露给 DI 容器。
 *
 * 为保持一致性，我们提供一个空的 Module 占位，或者仅提供配置加载。
 * 实际上，mq 包更适合作为统一入口。
 * ======================================================================== */

// 此处暂不需要 Module，因为 factory.go 的 init() 已经处理了注册。
// 且具体的 Producer/Consumer 创建通常由 mq.Factory 根据 Config 动态创建。
